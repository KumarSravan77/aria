# OPA Gatekeeper

Gatekeeper is included as an admission-policy option. Use it when teams prefer Rego/OPA constraints.

Kyverno is the default policy engine in this repo because Kubernetes YAML policies are easier for platform teams to maintain.
